#include "structura.h"
void creare_polinom(pol **head, int putere_max);
pol *creare_cap(pol *list_head,int putere);
pol *adaugare_final(pol *list_head, int putere);
void creare_suma(pol *p1_head, pol *p2_head, pol **psum_head, int putere);
